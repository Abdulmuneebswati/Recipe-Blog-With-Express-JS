<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="title">{{title}}</title>
    <link rel="stylesheet" href="http://localhost:4000/css/style.css">
</head>

<body>
    
        <img src="http://localhost:4000/uploads/{{recipe.image}}" class="img-fluid sticky-top" alt="{{recipe.name}}" loading="lazy">
    </div>
    <div class="col-12 col-md-8">
        <div class="row">
            <div class="col-12"><h1>{{recipe.name}}</h1></div>
            <div class="col-12 mb-4"><i class="bi bi-tag"></i>{{recipe.category}}</div>
            <div class="col-12" style="white-space:pre-line;">
                <h4>Cooking Instructions</h4>
                {{recipe.description}}
            </div>
        </div>
        <div class="row pt-4">
            <div class="col-12">
                <h4>Ingredients</h4>
                <ul class="list-group list-group-flush">
                    {{#each recipe.ingredients}}
                    <li class="list-group-item">{{this}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
</div>



        <footer class="py-5px mt-3">
            <p>Build By Muneeb</p>
        </footer>
    </div>
</body>


</html>